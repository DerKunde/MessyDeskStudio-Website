import './TopBar.css'

function TopBar() {
  return (
    <header className="topbar">
      <nav className="topbar-nav">
        <a href="/" className="topbar-home-btn">Home</a>
      </nav>
    </header>
  )
}

export default TopBar